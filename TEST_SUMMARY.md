# 摄像头功能测试与问题排查

## ✅ 已完成的修改

### 1. USB摄像头过滤增强
- 新增关键词：webcam, hd camera, ir camera, builtin
- 过滤未命名设备
- 详细日志显示过滤结果

### 2. 设备状态窗口重新设计
**新布局（左右分栏）：**
```
┌──────────────────────────────────────┐
│ 左手环          │ 左手摄像头          │
│  🔋 56%        │  未配置             │
│  ● 已连接      │  [预览] 按钮        │
│  ● 空闲        │                     │
├──────────────────────────────────────┤
│ 右手环          │ 右手摄像头          │
│  🔋 --         │  未配置             │
│  ● 未连接      │  [预览] 按钮        │
│  ● 空闲        │                     │
├──────────────────────────────────────┤
│ 磁盘空间                              │
│  129.83 GB | 52.9 %                  │
└──────────────────────────────────────┘
```

### 3. 推流逻辑修复
- 只启动已配置的摄像头
- 分别显示左右手摄像头状态
- 单摄像头场景自动分配给左右手

### 4. 预览功能
- 每侧独立预览按钮
- 推流时才显示按钮
- 点击弹窗查看对应摄像头画面

---

## ❓ 待排查问题

### 问题：配置文件加载后Stage下拉框为空

**症状：**
- 右上角"当前Stage"下拉框为空
- 无论加载什么配置文件都一样

**排查步骤：**

1. **打开浏览器控制台（F12）**
   - 查看是否有JavaScript错误

2. **检查配置加载日志**
   - 加载配置文件后，查找以下日志：
   ```
   [Collection] 加载采集配置: {...}
   [Collection] Stage数量: X
   ```

3. **手动检查stages数组**
   - 在控制台输入：
   ```javascript
   window.collectionController.stages
   ```
   - 应该返回一个数组，包含stage对象

4. **检查updateStageSelect是否执行**
   - 在控制台输入：
   ```javascript
   window.collectionController.updateStageSelect()
   ```
   - 手动触发更新

5. **检查DOM元素**
   - 在控制台输入：
   ```javascript
   document.getElementById('stageSwitchSelect')
   ```
   - 应该返回select元素对象

**可能原因：**
1. stages数组确实为空（配置文件格式问题）
2. updateStageSelect()未执行或执行时机有问题
3. DOM元素ID不匹配
4. JavaScript错误阻止了后续代码执行

**临时解决方案：**
如果手动调用`window.collectionController.updateStageSelect()`能解决，说明是初始化时机问题。

---

## 🧪 摄像头功能测试清单

### 准备
- [ ] 连接USB摄像头（至少1个）
- [ ] 启动系统：`npm start`
- [ ] 打开浏览器控制台（F12）

### 测试1：USB摄像头过滤
- [ ] 点击底部"摄像头控制"的"启动推流"按钮
- [ ] 查看控制台日志：
  ```
  [CameraControl] 找到 X 个摄像头设备 (过滤后 Y 个USB摄像头)
  ```
- [ ] 确认系统自带摄像头被过滤（显示[已过滤]）

### 测试2：单摄像头自动配置
- [ ] 配置弹窗中，左右手下拉列表都自动选中USB摄像头
- [ ] 点击"应用配置"

### 测试3：状态显示
- [ ] 右上角设备状态窗口：
  - 左手环 | 左手摄像头（显示"已配置"）
  - 右手环 | 右手摄像头（显示"已配置"）

### 测试4：推流
- [ ] 推流启动后：
  - 左手摄像头显示"推流中"
  - 右手摄像头显示"推流中"
  - 显示"预览"按钮

### 测试5：预览
- [ ] 点击左手的"预览"按钮
- [ ] 弹窗显示左侧摄像头画面
- [ ] 点击右手的"预览"按钮
- [ ] 弹窗显示右侧摄像头画面

### 测试6：录制（完整流程）
- [ ] 开始采集任务
- [ ] 按下空格键（第一次）
- [ ] 控制台显示"摄像头录制已启动"
- [ ] 采集结束后下载2个webm文件

---

## 📞 如何报告问题

请提供以下信息：

1. **控制台截图**
   - F12打开控制台
   - 截图所有红色错误信息

2. **配置文件内容**
   - 分享无法加载的配置文件JSON

3. **操作步骤**
   - 详细描述如何复现问题

4. **预期vs实际**
   - 预期看到什么
   - 实际看到什么

---

## 🔧 调试命令

在浏览器控制台输入以下命令进行调试：

```javascript
// 检查stages数组
window.collectionController.stages

// 手动触发Stage下拉框更新
window.collectionController.updateStageSelect()

// 检查当前配置
window.collectionController.collectionConfig

// 检查摄像头状态
window.cameraControl.getStatus()

// 检查已选择的摄像头
window.cameraControl.selectedCameras
```

