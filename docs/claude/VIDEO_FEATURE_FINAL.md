# 视频录制功能最终确认

## 📋 用户需求总结

你的使用场景：
- 多个受试者，每个人采集多个 h5
- 经常点击"预览"检查摄像头是否正常工作
- 不需要在录制时同时预览

## ✅ 当前实现完全满足需求！

### 功能 1：摄像头预览 ✅

**位置**：设备状态悬浮窗口（页面右下角）

**功能**：
- 左手摄像头状态显示 + "预览"按钮
- 右手摄像头状态显示 + "预览"按钮
- 点击预览按钮 → 弹出全屏弹窗显示实时画面
- 支持同时预览左右手摄像头

**实现细节**：
```
设备状态窗口
├── 左手环状态
├── 左手摄像头状态
│   ├── 状态文字（已配置 / 推流中 / 录制中）
│   └── [预览] 按钮 ← 点击这里
├── 右手环状态
└── 右手摄像头状态
    ├── 状态文字
    └── [预览] 按钮
```

**代码位置**：
- HTML: `index.html:5336, 5376` (预览按钮)
- HTML: `index.html:6153-6175` (预览弹窗)
- JavaScript: `camera-ui.js:370-416` (预览功能实现)

---

### 功能 2：第一次空格触发录制 ✅

**实现**：
- 使用 `_cameraRecordingStarted` 标志位
- 只有第一次按空格键时启动录制
- 后续空格键不再重复触发

**代码位置**：`collection-controller.js:3494-3498`

---

### 功能 3：h5 保存时同步停止 mp4 ✅

**实现**：
- `onCollectionStop()` 中先停止视频，再关闭 h5
- 同时处理左右手摄像头

**代码位置**：`realtimeEngine.js:375-422`

---

## 🎯 完整工作流程

### 典型使用场景：

```
【受试者 A - 第一个采集】
1. 打开采集软件
2. 点击"摄像头配置" → 选择左手/右手摄像头 → 应用配置
3. 点击设备状态窗口的"预览"按钮 → 确认画面正常 → 关闭预览
4. 开始采集
5. 按第一次空格键 → 🎥 开始录制视频
6. 按更多次空格键 → 仅记录同步信号，不重复录制
7. 完成采集 → 自动停止录制并保存 h5 和 mp4

【受试者 A - 第二个采集】
8. 点击"预览"按钮 → 确认摄像头仍正常 → 关闭预览
9. 开始第二次采集
10. 重复步骤 5-7

【受试者 B】
11. 更换受试者
12. 点击"预览"按钮 → 确认摄像头正常 → 关闭预览
13. 重复步骤 4-7
```

---

## 🔍 预览功能详细说明

### 预览按钮的显示条件

预览按钮会在以下情况下显示：
- 摄像头已配置
- 摄像头正在推流（调用了 `startStreaming()`）

### 预览流程

1. **打开预览**：
   ```javascript
   点击"预览"按钮
   → 检查摄像头是否推流
   → 弹出预览窗口
   → 将视频流绑定到 <video> 元素
   → 显示实时画面
   ```

2. **关闭预览**：
   ```javascript
   点击"关闭"按钮
   → 停止 <video> 播放
   → 释放 video.srcObject
   → 隐藏预览窗口
   ```

3. **关键点**：
   - 预览使用浏览器的 `getUserMedia()` 直接访问摄像头
   - 关闭预览后摄像头仍被浏览器占用（但 video 元素不再显示）
   - 开始录制时，camera_server 通过 ffmpeg 独立访问摄像头

---

## ⚠️ 注意事项

### 1. 摄像头占用问题

**问题**：同一个摄像头可能被多个程序占用导致冲突

**最佳实践**：
- ✅ 配置完摄像头后，使用预览确认正常
- ✅ 确认后关闭预览
- ✅ 开始采集前不要再打开预览
- ✅ 录制过程中不要打开预览（会导致冲突）

### 2. 推流状态

**重要**：预览按钮只有在摄像头"推流中"时才显示

如何启动推流：
1. 方式 1：配置摄像头后，自动启动推流（如果实现了）
2. 方式 2：在采集页面，摄像头会自动推流

### 3. 录制和预览的关系

```
【正确的使用顺序】
1. 配置摄像头
2. 短暂预览确认
3. 关闭预览
4. 开始采集 → 自动推流
5. 按空格键 → camera_server 开始录制
6. 完成采集 → 停止录制

【避免的情况】
❌ 录制时打开预览（可能冲突）
❌ 预览时开始录制（可能冲突）
```

---

## 🎉 结论

**当前实现完全满足你的需求！**

✅ 预览功能：设备状态窗口 → 点击"预览"按钮 → 查看实时画面
✅ 第一次空格触发录制：防重复触发机制正确
✅ h5 保存时同步停止 mp4：停止顺序正确
✅ 适合多受试者采集场景：可以随时预览检查摄像头

你现在就可以：
1. 打开浏览器 http://localhost:3000
2. 配置左手摄像头（HP HD Camera 或 USB Camera）
3. 在设备状态窗口点击"预览"按钮
4. 看到实时画面，确认正常
5. 关闭预览，开始采集测试

服务器正在后台运行，随时可以测试！
